# Assert that a character vector only contains PKNCA parameter names

Assert that a character vector only contains PKNCA parameter names

## Usage

``` r
assert_param_name(param)
```

## Arguments

- param:

  A vector of parameter names to check

## Value

`param` or give an informative error
