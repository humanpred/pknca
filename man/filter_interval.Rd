% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pk.calc.all.R
\name{filter_interval}
\alias{filter_interval}
\title{Subset data to the rows used for calculations within an interval}
\usage{
filter_interval(data, start, end, include_na = FALSE, include_end = TRUE)
}
\arguments{
\item{data}{A data.frame with a column named \code{time} (and, for duration data,
a column named \code{duration}, which is ignored during selection)}

\item{start, end}{The beginning and end times of the interval}

\item{include_na}{Should rows with an \code{NA} \code{time} be kept?}

\item{include_end}{Should a row with \code{time == end} be kept?}
}
\value{
The rows of \code{data} selected for the interval
}
\description{
Rows are selected by their \code{time} falling within the interval: \code{start <= time} and \code{time <= end} (or \code{time < end} when \code{include_end=FALSE}).  For
duration data (for example, urine collections or intravenous infusions),
\code{time} is the start of the collection or administration, and the \code{duration}
column is not consulted during selection: a record whose duration starts
within the interval and ends after the interval \code{end} is selected and
contributes its full amount to calculations within the interval.  For the
simplest interpretation of results, align collection start and end times
with interval boundaries.
}
\keyword{Internal}
