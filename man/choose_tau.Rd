% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/choose.intervals.R
\name{choose_tau}
\alias{choose_tau}
\title{Choose the dosing interval from the times doses were given}
\usage{
choose_tau(
  time.dosing,
  tau.typical = NULL,
  tau.tolerance = NULL,
  options = list()
)
}
\arguments{
\item{time.dosing}{Times when doses were given.}

\item{tau.typical}{Dosing intervals to consider, in the time units of the
data.  The default (see \code{\link[=PKNCA.options]{PKNCA.options()}}) assumes hours and covers
four-hourly through four-weekly dosing.}

\item{tau.tolerance}{How far the spacing between doses may be from a typical
interval and still be treated as that interval, as a fraction of it.  The
default of 0.1 accepts a nominally daily dose given between 21.6 and 26.4
hours after the last one.}

\item{options}{List of changes to the default PKNCA options (see
\code{PKNCA.options()})}
}
\value{
The dosing interval, or \code{NA} when fewer than two doses were given
and nothing repeats.
}
\description{
Doses recorded at the time they were actually given are not evenly spaced:
a nominally daily dose may be given 22.5 hours after the last one and 24.3
hours after that.  This finds the interval the doses were meant to repeat
on, by taking the typical spacing between them and, when it is close enough
to one of the intervals drugs are usually given on, using that.
}
\details{
Doses that are exactly evenly spaced give that spacing, whatever
\code{tau.typical} holds, so nominal times are unaffected.  Otherwise the
median spacing is compared with \code{tau.typical} and the closest is used when
it is within \code{tau.tolerance}.  When none is close enough the median
spacing itself is returned with a warning, because the doses repeat on
something that is not a usual interval.

The median is used rather than the mean so that a missed or a late dose
does not move the answer.

\strong{\code{tau.typical} is in the time units of the data.}  The default assumes
hours.  For data recorded in days or minutes, set the option to match, or
the snapping will not do anything useful.
}
\examples{
# Nominal daily dosing
choose_tau(c(0, 24, 48, 72))

# The same doses as actually given
choose_tau(c(0, 22.5, 48.3, 71.2))

# Doses that repeat on something that is not a usual interval
suppressWarnings(choose_tau(c(0, 20, 40, 60)))
}
\seealso{
\code{\link[=find.tau]{find.tau()}}, which finds an interval that the doses repeat on
exactly, and \code{\link[=choose.auc.intervals]{choose.auc.intervals()}}

Other Interval determination:
\code{\link[=choose.auc.intervals]{choose.auc.intervals()}},
\code{\link[=find.tau]{find.tau()}},
\code{\link[=resolve_dose_tau]{resolve_dose_tau()}}
}
\concept{Interval determination}
