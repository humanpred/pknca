% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioequivalence.R
\name{fitbe_models}
\alias{fitbe_models}
\title{Fit bioequivalence mixed-effects models for NCA endpoints}
\usage{
fitbe_models(
  object,
  endpoints = c("cmax", "aucinf.obs", "aucinf.pred", "auclast"),
  reference_col,
  reference_value,
  fixed = NULL,
  random = NULL
)
}
\arguments{
\item{object}{A \code{PKNCAresults} object (preferred) or a data.frame in the long
format produced by calling \code{as.data.frame()} on a \code{PKNCAresults} object (a
\code{PPTESTCD} column of parameter names and a \code{PPORRES} or \code{PPSTRES} column of
values, alongside the grouping columns).}

\item{endpoints}{Character vector of NCA parameters (matched against
\code{PPTESTCD}) to model.}

\item{reference_col}{The name of the column identifying the
formulation/treatment.}

\item{reference_value}{The value of \code{reference_col} that is the reference
formulation.}

\item{fixed}{Optional fixed-effects right-hand side as a single string (for
example \code{"sequence + period + treatment"}).  When \code{NULL}, it is derived from
the grouping variables of a \code{PKNCAresults} object.}

\item{random}{Optional random-effects specification as a single string (for
example \code{"(1|subject)"}).  When \code{NULL}, it is derived from the subject
column.}
}
\value{
An object of class \code{fitbe_models}: a list with elements \code{models} (the
per-endpoint \code{\link[lmerTest:lmer]{lmerTest::lmer()}} fits), \code{fixed} (a data.frame of fixed-effect
coefficients with Satterthwaite degrees of freedom), \code{variance} (a
data.frame of variance components from \code{\link[lme4:VarCorr]{lme4::VarCorr()}}), and the
\code{reference_col}, \code{reference_value}, and \code{value_col} that were used.
}
\description{
\code{fitbe_models()} fits a linear mixed-effects model to the log-transformed
value of each requested NCA endpoint, the standard analysis for average
bioequivalence of a crossover study.  The reference formulation is set as the
first factor level so that downstream contrasts estimate test-versus-reference
ratios.
}
\details{
The model fit for each endpoint is \verb{log(value) ~ <fixed> + <random>} using
\code{\link[lmerTest:lmer]{lmerTest::lmer()}} so that Satterthwaite degrees of freedom are available.
When \code{fixed} and \code{random} are not supplied, they are derived from the grouping
structure of a \code{PKNCAresults} object: the subject column becomes the random
intercept (\code{(1|subject)}) and the remaining grouping columns (always including
\code{reference_col}) become the fixed effects.  Automatic derivation requires a
\code{PKNCAresults} object; when a plain data.frame is supplied, \code{fixed} (and
usually \code{random}) must be given.
}
\seealso{
\code{\link[=fitbe_table]{fitbe_table()}} to summarize the fits and \code{\link[=fitbe_calculate]{fitbe_calculate()}} for a
one-step wrapper.

Other Bioequivalence:
\code{\link[=be_assess]{be_assess()}},
\code{\link[=be_compare]{be_compare()}},
\code{\link[=be_design]{be_design()}},
\code{\link[=be_expand_limits]{be_expand_limits()}},
\code{\link[=be_regulator]{be_regulator()}},
\code{\link[=be_within_var]{be_within_var()}},
\code{\link[=fitbe_calculate]{fitbe_calculate()}},
\code{\link[=fitbe_table]{fitbe_table()}}
}
\concept{Bioequivalence}
