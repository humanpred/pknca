% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pk.calc.simple.R
\name{pk.calc.tmin}
\alias{pk.calc.tmin}
\title{Determine time of minimum observed PK concentration}
\usage{
pk.calc.tmin(conc, time, options = list(), first.tmin = NULL, check = TRUE)
}
\arguments{
\item{conc}{Measured concentrations}

\item{time}{Time of the measurement of the concentrations}

\item{options}{List of changes to the default PKNCA options (see
\code{PKNCA.options()})}

\item{first.tmin}{If there is more than one time point with the minimum value (Cmin),
which time should be selected for Tmin?  If 'TRUE', the first will be selected. If not,
then the last is considered Tmin.}

\item{check}{Run \code{\link[=assert_conc_time]{assert_conc_time()}}?}
}
\value{
The time of the minimum concentration
}
\description{
Input restrictions are:
\enumerate{
\item the \code{conc} and \code{time} must be the same length,
\item the \code{time} may have no NAs,
}
\code{NA} will be returned if:
\enumerate{
\item the length of \code{conc} and \code{time} is 0
\item all \code{conc} is \code{NA}
}
}
\examples{
conc_data <- Theoph[Theoph$Subject == 1,]
pk.calc.tmin(conc = conc_data$conc, time = conc_data$Time)
}
\seealso{
Other NCA time parameters: 
\code{\link{pk.calc.tlag}()},
\code{\link{pk.calc.tlast}()},
\code{\link{pk.calc.tmax}()}
}
\concept{NCA time parameters}
