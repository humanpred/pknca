% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/interval-table.R
\name{pknca_interval_table}
\alias{pknca_interval_table}
\title{Build an interval specification from a description of the analysis}
\usage{
pknca_interval_table(
  start,
  end,
  dosing = "single",
  route = "extravascular",
  sample_type = "spot",
  sparse = FALSE,
  tier = "common",
  include = NULL,
  exclude = NULL,
  impute = NULL,
  preset = NULL,
  clast_type = "obs",
  ...
)
}
\arguments{
\item{start, end}{The start and end time of the interval.  Both may be
vectors, giving one set of rows per interval.}

\item{dosing}{Was the drug given once (\code{"single"}), repeatedly without
assuming steady state (\code{"multiple"}), or repeatedly at steady state
(\code{"steady_state"})?  Asking for \code{"steady_state"} also gives the parameters
that apply to any repeated dose.  See \code{\link[=pknca_dosing]{pknca_dosing()}}.}

\item{route}{How the drug was given; see \code{\link[=pknca_routes]{pknca_routes()}}.}

\item{sample_type}{Were concentrations measured in samples taken at a point
in time (\code{"spot"}, the usual case for blood, plasma, and serum) or in a
collection over an interval (\code{"interval"}, the usual case for urine and
feces)?  See \code{\link[=pknca_sample_types]{pknca_sample_types()}}.}

\item{sparse}{Is this a sparse sampling design?  The parameters chosen are
the same either way -- \code{auclast} and \code{aumclast} are estimated with the
sparse methods when the data are sparse, and the rest are calculated from
the arithmetic-mean profile -- but a sparse design imputes nothing.}

\item{tier}{\code{"common"} (the default) gives the parameters usually reported
for the context; \code{"all"} gives every parameter it can calculate.}

\item{include, exclude}{NCA parameters or concepts (see \code{\link[=pknca_concepts]{pknca_concepts()}})
to add to or remove from what the context gives.  A parameter named in
both is an error.}

\item{impute}{The imputation to use, as for \code{\link[=PKNCAdata]{PKNCAdata()}}.  The default of
\code{NULL} chooses one from the context; \code{NA} uses none.}

\item{preset}{A named set of arguments to start from; see
\code{\link[=pknca_presets]{pknca_presets()}}.  Arguments given explicitly override the preset.}

\item{clast_type}{Should the extrapolation to infinity use the observed
(\code{"obs"}) or predicted (\code{"pred"}) last concentration?}

\item{...}{Columns to add to every row, typically the groups the interval
applies to.}
}
\value{
A data.frame with one or more rows per interval:  \code{start}, \code{end}, a
logical column per parameter, an \code{impute} column when any imputation
applies, and any columns given in \code{...}.  An interval is split into more
than one row when some of its parameters must not be calculated from the
imputed data.
}
\description{
Chooses the NCA parameters to calculate for an interval, and the imputation
to calculate them from, given when the interval runs and how the drug was
given.  The result is a data.frame suitable for the \code{intervals} argument of
\code{\link[=PKNCAdata]{PKNCAdata()}}.
}
\details{
The interval is assumed to start at a dose, which is what makes an
imputation at the start meaningful.  Pass \code{impute = NA} for an interval
that starts partway through a profile.

Which AUC the interval is built on follows from \code{dosing}:  a single dose
uses AUClast and the extrapolation to infinity, and a repeated dose uses
the AUCint family, which interpolates at both interval boundaries.  The
clearance, volume, and mean residence time that follow from that AUC are
chosen to match.
}
\examples{
# A single oral dose
pknca_interval_table(0, 24, dosing = "single", route = "extravascular")

# At steady state, with the fluctuation parameters added
pknca_interval_table(
  144, 168,
  dosing = "steady_state", route = "extravascular",
  include = "fluctuation"
)

# A urine collection
pknca_interval_table(0, 24, dosing = "single", route = "extravascular",
                     sample_type = "interval")
}
\seealso{
\code{\link[=pknca_parameter_table]{pknca_parameter_table()}} for how each parameter is classified,
\code{\link[=PKNCAdata]{PKNCAdata()}}, and the vignette "Selection of Calculation Intervals"

Other Interval specifications:
\code{\link[=add.interval.col]{add.interval.col()}},
\code{\link[=check.interval.specification]{check.interval.specification()}},
\code{\link[=choose.auc.intervals]{choose.auc.intervals()}},
\code{\link[=get.interval.cols]{get.interval.cols()}},
\code{\link[=get.parameter.deps]{get.parameter.deps()}},
\code{\link[=interval_add_impute]{interval_add_impute()}},
\code{\link[=interval_add_param]{interval_add_param()}},
\code{\link[=interval_add_secondary]{interval_add_secondary()}},
\code{\link[=pknca_check_parameter_classification]{pknca_check_parameter_classification()}},
\code{\link[=pknca_concepts]{pknca_concepts()}},
\code{\link[=pknca_parameter_table]{pknca_parameter_table()}},
\code{\link[=pknca_presets]{pknca_presets()}},
\code{\link[=pknca_ref]{pknca_ref()}}
}
\concept{Interval specifications}
