% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioequivalence.R
\name{be_design}
\alias{be_design}
\title{Classify a bioequivalence crossover design}
\usage{
be_design(data, subject, sequence, period, treatment, reference_value)
}
\arguments{
\item{data}{A long data.frame with one row per concentration-derived
observation (subject, period, treatment).}

\item{subject, sequence, period, treatment}{Column names (length-1 character)
identifying the subject, randomization sequence, period, and treatment.
\code{sequence} may be \code{NA} to derive it from the treatment-by-period pattern.}

\item{reference_value}{The value of \code{treatment} that is the reference
formulation.}
}
\value{
An object of class \code{be_design}: a list with elements \code{design} (one
of \code{"2x2x2"}, \code{"full_replicate"}, \code{"partial_replicate"}, \code{"other"}),
\code{n_sequences}, \code{n_periods}, \code{n_treatments}, \code{n_subjects}, \code{sequences},
\code{treatments}, \code{reference}, \code{replicate_reference}, \code{replicate_test},
\code{reps_reference}, \code{reps_test}, \code{balanced}, and \code{feasible} (a named logical
vector for \code{abe}, \code{abel}, \code{rsabe}, \code{ntid}, \code{hvntid}).
}
\description{
\code{be_design()} inspects the treatment-by-period pattern of a crossover study
and reports the design type, the replication of the reference and test
formulations, and which regulatory frameworks are feasible.  Reference
scaling (EMA/HC/GCC/FDA) requires the reference to be replicated; the narrow
therapeutic index frameworks additionally require the test to be replicated.
}
\examples{
d <- data.frame(
  subject = rep(1:4, each = 4),
  period = rep(1:4, times = 4),
  sequence = rep(c("TRTR", "RTRT"), each = 8),
  treatment = c("T", "R", "T", "R", "R", "T", "R", "T",
                "T", "R", "T", "R", "R", "T", "R", "T")
)
be_design(d, "subject", "sequence", "period", "treatment", reference_value = "R")
}
\seealso{
\code{\link[=be_assess]{be_assess()}}

Other bioequivalence: 
\code{\link{be_assess}()},
\code{\link{be_compare}()},
\code{\link{be_expand_limits}()},
\code{\link{be_regulator}()},
\code{\link{be_within_var}()},
\code{\link{fitbe_calculate}()},
\code{\link{fitbe_models}()},
\code{\link{fitbe_table}()}
}
\concept{bioequivalence}
