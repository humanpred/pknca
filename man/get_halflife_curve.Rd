% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/half.life.R
\name{get_halflife_curve}
\alias{get_halflife_curve}
\title{Interpolate and extrapolate concentrations along the half-life fit}
\usage{
get_halflife_curve(
  object,
  tout = NULL,
  n = 50,
  extrapolate_earlier = FALSE,
  extrapolate_later = TRUE
)
}
\arguments{
\item{object}{A PKNCAresults or PKNCAdata object}

\item{tout}{Times for output.  The same times are used for every group and
interval.  If \code{NULL} (the default), \code{n} equally-spaced times spanning the
concentrations used for the fit are used instead.}

\item{n}{The number of equally-spaced times to generate when \code{tout} is
\code{NULL}.  It is ignored when \code{tout} is given.}

\item{extrapolate_earlier, extrapolate_later}{Should concentrations be
extrapolated before the first (\code{extrapolate_earlier}) or after the last
(\code{extrapolate_later}) concentration used for the fit?  Times outside the
fit that are not extrapolated give an \code{NA} concentration.}
}
\value{
A data.frame with the grouping columns, the interval \code{start} and
\code{end} times, and the columns:
\itemize{
\item \code{time}: the time of the concentration, on the same scale as the times in
the concentration data
\item \code{conc}: the concentration on the half-life fit at that time
}

\code{conc} is \code{NA} where the half-life could not be calculated or was excluded,
and where extrapolation was requested but not allowed.  Groups without a
fit give a single row with an \code{NA} \code{time} when \code{tout} is \code{NULL}, since no
times can be generated for them.
}
\description{
Concentrations are given as concentrations, not log-concentrations.  Take the
natural log of \code{conc} if the log-concentration is wanted.
}
\details{
Like \code{\link[stats:approx]{stats::approx()}}, give either \code{tout} for specific times or \code{n} for
equally-spaced times.  With \code{n}, the times span the concentrations used for
the fit (\code{time_first} to \code{time_last} from \code{\link[=get_halflife_fit]{get_halflife_fit()}}), so \code{tout} is
required to extrapolate.
}
\examples{
o_conc <- PKNCAconc(Theoph, conc~Time|Subject)
o_data <- PKNCAdata(o_conc, intervals = data.frame(start = 0, end = Inf, half.life = TRUE))
o_nca <- pk.nca(o_data)
# Equally-spaced times across the fit
head(get_halflife_curve(o_nca))
# Specific times, extrapolating past the last concentration used
get_halflife_curve(o_nca, tout = c(12, 24, 36))
}
\seealso{
\code{\link[=get_halflife_fit]{get_halflife_fit()}} for the slope and intercept of the fit, and
\code{\link[=get_halflife_points]{get_halflife_points()}} for the concentrations used for it
}
