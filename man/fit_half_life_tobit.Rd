% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/half.life.R
\name{fit_half_life_tobit}
\alias{fit_half_life_tobit}
\title{Perform a Tobit half-life fit given the data.  The function fits the data
using maximum likelihood without any point selection or validation.}
\usage{
fit_half_life_tobit(data, tlast, optim_control = list())
}
\arguments{
\item{data}{The data to fit.  Must have columns named \code{"log_conc"},
\code{"time"}, \code{"log_lloq"}, and \code{"mask_blq"}.  \code{log_conc} for BLQ
observations is not used (the likelihood uses \code{log_lloq} instead).}

\item{tlast}{The time of last observed concentration above the lower limit
of quantification.}

\item{optim_control}{A list of control parameters passed to \code{\link[stats:optim]{stats::optim()}}.}
}
\value{
A data.frame with one row and columns named \code{"lambda.z"},
\code{"clast.pred"}, \code{"lambda.z.time.first"}, \code{"lambda.z.time.last"},
\code{"lambda.z.n.points"}, \code{"lambda.z.n.points_blq"}, \code{"half.life"},
\code{"span.ratio"}, \code{"tobit_residual"}, and \code{"adj_tobit_residual"}.
Returns \code{NA} for all columns if \code{\link[stats:optim]{stats::optim()}} does not converge,
and emits a warning.
}
\description{
Perform a Tobit half-life fit given the data.  The function fits the data
using maximum likelihood without any point selection or validation.
}
\seealso{
\code{\link[=pk.calc.half.life]{pk.calc.half.life()}}, \code{fit_half_life_tobit_LL()}
}
