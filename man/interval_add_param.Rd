% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervals_support.R
\name{interval_add_param}
\alias{interval_add_param}
\alias{interval_remove_param}
\title{Add or remove parameters in the intervals of a PKNCAdata object}
\usage{
interval_add_param(
  data,
  param = NULL,
  param_pattern = NULL,
  target_groups = NULL,
  ...
)

interval_remove_param(
  data,
  param = NULL,
  param_pattern = NULL,
  target_groups = NULL,
  ...
)
}
\arguments{
\item{data}{A \code{PKNCAdata} object or a data.frame of intervals.}

\item{param}{A character vector of NCA parameter names.}

\item{param_pattern}{A character vector of regular expressions matching NCA
parameter names.  One or both of \code{param} and \code{param_pattern} must be given.}

\item{target_groups}{A data.frame of group values restricting the change to
matching intervals.  Every column must match (and) for at least one row of
\code{target_groups} (or).  \code{NULL} (the default) applies the change to all
intervals.}

\item{...}{Ignored.}
}
\value{
The input with the parameters added or removed.
}
\description{
Add or remove parameters in the intervals of a PKNCAdata object
}
\examples{
intervals <- data.frame(start = 0, end = 24, cmax = TRUE, tmax = TRUE)
interval_add_param(intervals, param = c("auclast", "half.life"))
# At least one parameter must remain, so removing every one is an error
interval_remove_param(intervals, param_pattern = "^tmax$")
}
\seealso{
\code{\link[=interval_add_impute]{interval_add_impute()}}, \code{\link[=get.interval.cols]{get.interval.cols()}}

Other Interval specifications:
\code{\link[=add.interval.col]{add.interval.col()}},
\code{\link[=check.interval.specification]{check.interval.specification()}},
\code{\link[=choose.auc.intervals]{choose.auc.intervals()}},
\code{\link[=get.interval.cols]{get.interval.cols()}},
\code{\link[=get.parameter.deps]{get.parameter.deps()}},
\code{\link[=interval_add_impute]{interval_add_impute()}}
}
\concept{Interval specifications}
