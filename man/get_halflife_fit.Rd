% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/half.life.R
\name{get_halflife_fit}
\alias{get_halflife_fit}
\title{Get the half-life fit line for each interval}
\usage{
get_halflife_fit(object)
}
\arguments{
\item{object}{A PKNCAresults or PKNCAdata object}
}
\value{
A data.frame with one row for each group and interval where
half-life was calculated.  Along with the grouping columns and the interval
\code{start} and \code{end} times, it has the columns:
\itemize{
\item \code{intercept}: the natural log of the concentration where the line crosses
time 0
\item \code{slope}: the slope of the line, \code{-lambda.z}
\item \code{time_first}, \code{time_last}: the first and last times of the concentrations
used for the fit
}

\code{intercept} and \code{slope} are \code{NA} when the half-life could not be
calculated or was excluded.
}
\description{
The half-life fit is the log-linear regression of concentration on time,
\code{log(conc) = intercept + slope*time}.  Concentrations along the line are
\code{exp(intercept + slope*time)}.
}
\details{
Times in a \code{PKNCAresults} object are relative to the start of the interval,
but \code{time_first}, \code{time_last}, and the time scale of \code{intercept} are on the
same scale as the times in the concentration data so that the line can be
drawn with the observed concentrations.
}
\examples{
o_conc <- PKNCAconc(Theoph, conc~Time|Subject)
o_data <- PKNCAdata(o_conc, intervals = data.frame(start = 0, end = Inf, half.life = TRUE))
o_nca <- pk.nca(o_data)
get_halflife_fit(o_nca)
}
\seealso{
\code{\link[=get_halflife_points]{get_halflife_points()}} to see which concentrations were used for
the fit
}
