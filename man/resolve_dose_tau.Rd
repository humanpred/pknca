% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/choose.intervals.R
\name{resolve_dose_tau}
\alias{resolve_dose_tau}
\title{Determine the dosing interval (tau) to use for a calculation interval}
\usage{
resolve_dose_tau(interval, time.dose, options = list())
}
\arguments{
\item{interval}{One row of an interval definition (see
\code{\link[=check.interval.specification]{check.interval.specification()}})}

\item{time.dose}{The dose times for the whole group (not just the interval;
an interval one \code{tau} long contains a single dose, so nothing repeats
within it)}

\item{options}{List of changes to the default PKNCA options (see
\code{PKNCA.options()})}
}
\value{
The dosing interval, or \code{NA_real_} when it cannot be determined
}
\description{
A \code{tau} column in the interval specification takes precedence over
detection so that designs where only the steady-state dose is present in the
dosing data (nothing repeats, so nothing can be detected) can still be
calculated.  Otherwise \code{tau} is detected from the group's dose times with
\code{\link[=find.tau]{find.tau()}}.
}
\seealso{
Other Interval determination:
\code{\link[=choose.auc.intervals]{choose.auc.intervals()}},
\code{\link[=choose_tau]{choose_tau()}},
\code{\link[=find.tau]{find.tau()}}
}
\concept{Interval determination}
\keyword{Internal}
