% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/001-add.interval.col.R
\name{pknca_concept}
\alias{pknca_concept}
\alias{pknca_concept<-}
\title{The concept a parameter calculation function computes}
\usage{
pknca_concept(x)

pknca_concept(x) <- value
}
\arguments{
\item{x}{A parameter calculation function (for example \code{pk.calc.cmax}).}

\item{value}{A concept name; see \code{\link[=pknca_concepts]{pknca_concepts()}} for the ones PKNCA uses.}
}
\value{
\code{pknca_concept()} gives the concept, or \code{NULL} when none is set.
The replacement form gives the function with the attribute set.
}
\description{
The concept is the kind of quantity a user asks for -- "clearance" rather
than the fifteen registered clearance parameters.  It is stored as an
attribute on the calculation function so that it sits with the code that
computes it, and so that a parameter added by another package can carry one
too.
}
\details{
A function that computes different concepts depending on which
parameter it is registered for -- \code{pk.calc.dn()} is the example within
PKNCA -- should have no concept.  Those parameters take the concept of the
parameter they depend on.  When neither applies, set the concept for the
individual parameter with the \code{selection} argument of
\code{\link[=add.interval.col]{add.interval.col()}}.
}
\examples{
pknca_concept(pk.calc.cmax)
}
\seealso{
\code{\link[=pknca_concepts]{pknca_concepts()}}, \code{\link[=add.interval.col]{add.interval.col()}}, and the vignette
"Writing Parameter Functions"
}
