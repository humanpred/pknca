% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioequivalence.R
\name{be_dataset}
\alias{be_dataset}
\title{Build and validate a bioequivalence dataset}
\usage{
be_dataset(
  object,
  reference_col,
  reference_value,
  endpoints = c("cmax", "aucinf.obs", "aucinf.pred", "auclast"),
  subject = NULL,
  sequence = NULL,
  period = NULL
)
}
\arguments{
\item{object}{A \code{PKNCAresults} object or a tidy long data.frame with a
\code{PPTESTCD} column of parameter names, a \code{PPORRES}/\code{PPSTRES} column of
values, and subject/sequence/period/treatment columns.}

\item{reference_col}{The column identifying the formulation/treatment.}

\item{reference_value}{The value of \code{reference_col} that is the reference
formulation.}

\item{endpoints}{Character vector of NCA parameters (matched against
\code{PPTESTCD}) to assess.}

\item{subject, sequence, period}{Column names for the subject, randomization
sequence, and period.  When \code{NULL} they are taken from the \code{PKNCAresults}
object or detected from common column names.  \code{sequence} may be absent.}
}
\value{
An object of class \code{be_dataset}: a list with \code{data} (the
standardized long frame, including a \code{.units} column), \code{columns} (the
resolved column names, including \code{units}), \code{reference_value}, \code{test_levels},
and \code{endpoints} (those present).
}
\description{
\code{be_dataset()} prepares a noncompartmental result for bioequivalence
calculation: it accepts a \code{PKNCAresults} object or a tidy long data.frame,
resolves the value column, drops excluded/invalid rows, detects the
subject/sequence/period columns, validates the reference, and sets the
reference formulation as the first factor level.  It standardizes the
modeling columns (\code{.subject}, \code{.sequence}, \code{.period}, \code{.trt}, \code{.logval}) used
by the downstream fitters.
}
\details{
The endpoint value is taken from the \code{PPSTRES} column when present, otherwise
\code{PPORRES}.  The measurement units are read from the matching units column --
\code{PPSTRESU} for \code{PPSTRES}, or \code{PPORRESU} for \code{PPORRES} -- which a \code{PKNCAresults}
object provides automatically; a plain data.frame supplies units the same way
by including the corresponding \code{PPSTRESU}/\code{PPORRESU} column.  When no units
column is present, units are unavailable and the \code{units} column is omitted
from the assessment table.
}
\seealso{
Other Bioequivalence:
\code{\link[=be_assess]{be_assess()}},
\code{\link[=be_compare]{be_compare()}},
\code{\link[=be_design]{be_design()}},
\code{\link[=be_expand_limits]{be_expand_limits()}},
\code{\link[=be_extract_param]{be_extract_param()}},
\code{\link[=be_fit_model_single]{be_fit_model_single()}},
\code{\link[=be_fit_models]{be_fit_models()}},
\code{\link[=be_regulator]{be_regulator()}},
\code{\link[=be_table]{be_table()}},
\code{\link[=be_within_var]{be_within_var()}}
}
\concept{Bioequivalence}
