% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/half.life.R
\name{fit_half_life_tobit_LL}
\alias{fit_half_life_tobit_LL}
\title{Negative log-likelihood for Tobit half-life regression}
\usage{
fit_half_life_tobit_LL(par, log_conc, time, mask_blq, log_lloq)
}
\arguments{
\item{par}{A 3-element numeric vector: \code{c(log_c0, lambda_z, log_resid_error)}}

\item{log_conc}{Natural log of observed concentration (may be \code{-Inf} for
BLQ; those values are not used when \code{mask_blq} is \code{TRUE})}

\item{time}{Numeric time vector}

\item{mask_blq}{Logical vector; \code{TRUE} where the observation is below the
LLOQ}

\item{log_lloq}{Natural log of the lower limit of quantification}
}
\value{
The negative sum of the log-likelihood (a scalar)
}
\description{
Helper function used by \code{fit_half_life_tobit()} via \code{\link[stats:optim]{stats::optim()}}.
For observations above the LLOQ, the normal density contributes to the
likelihood.  For censored (BLQ) observations, the normal CDF up to the
LLOQ contributes.
}
\seealso{
\code{fit_half_life_tobit()}
}
