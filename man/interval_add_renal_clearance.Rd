% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/secondary-parameters.R
\name{interval_add_renal_clearance}
\alias{interval_add_renal_clearance}
\alias{interval_add_accumulation_ratio}
\alias{interval_add_metabolite_ratio}
\title{Link the common secondary parameters to their reference intervals}
\usage{
interval_add_renal_clearance(
  data,
  reference,
  param = "clr.obs",
  target_groups = NULL,
  ...
)

interval_add_accumulation_ratio(
  data,
  ref_start,
  ref_end,
  param = "ratio.aucint.last",
  target_groups = NULL,
  ...
)

interval_add_metabolite_ratio(
  data,
  reference,
  param = "ratio.aucinf.obs",
  target_groups = NULL,
  ...
)
}
\arguments{
\item{data}{A \code{PKNCAdata} object or a data.frame of intervals.}

\item{reference}{A data.frame of group values identifying the reference
profile (for example \code{data.frame(PCSPEC = "plasma")} or
\code{data.frame(Analyte = "parent")}).}

\item{param}{The name of one secondary NCA parameter (see
\code{\link[=pknca_parameter_table]{pknca_parameter_table()}} for which parameters are secondary).}

\item{target_groups}{A data.frame of group values restricting the parameter
request to matching intervals, with the same matching rules as \code{reference}.
\code{NULL} (the default) requests it on every interval that is not a reference
interval.}

\item{...}{Passed to \code{\link[=interval_add_secondary]{interval_add_secondary()}}, which accepts \code{ref_id}.}

\item{ref_start, ref_end}{The start and end times of the reference dosing
interval.}
}
\value{
The input with the parameter requested and the linkage columns set.
}
\description{
Each of these is \code{\link[=interval_add_secondary]{interval_add_secondary()}} with the reference specification
the analysis implies:  the profile the drug is cleared from for a renal
clearance, the first dosing interval for an accumulation ratio, and the
parent analyte for a metabolite ratio.
}
\examples{
intervals <-
  data.frame(
    PCSPEC = c("plasma", "urine"),
    start = 0, end = 24,
    aucinf.obs = c(TRUE, FALSE),
    ae = c(FALSE, TRUE)
  )
interval_add_renal_clearance(intervals, reference = data.frame(PCSPEC = "plasma"))
}
\seealso{
\code{\link[=interval_add_secondary]{interval_add_secondary()}}

Other Interval specifications:
\code{\link[=add.interval.col]{add.interval.col()}},
\code{\link[=check.interval.specification]{check.interval.specification()}},
\code{\link[=choose.auc.intervals]{choose.auc.intervals()}},
\code{\link[=get.interval.cols]{get.interval.cols()}},
\code{\link[=get.parameter.deps]{get.parameter.deps()}},
\code{\link[=interval_add_impute]{interval_add_impute()}},
\code{\link[=interval_add_param]{interval_add_param()}},
\code{\link[=interval_add_secondary]{interval_add_secondary()}},
\code{\link[=pknca_check_parameter_classification]{pknca_check_parameter_classification()}},
\code{\link[=pknca_concepts]{pknca_concepts()}},
\code{\link[=pknca_interval_table]{pknca_interval_table()}},
\code{\link[=pknca_parameter_table]{pknca_parameter_table()}},
\code{\link[=pknca_presets]{pknca_presets()}},
\code{\link[=pknca_ref]{pknca_ref()}}
}
\concept{Interval specifications}
