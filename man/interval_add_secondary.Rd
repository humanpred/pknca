% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/secondary-parameters.R
\name{interval_add_secondary}
\alias{interval_add_secondary}
\title{Link a secondary parameter to the interval it is calculated against}
\usage{
interval_add_secondary(
  data,
  param,
  reference = NULL,
  target_groups = NULL,
  ref_id = NULL,
  ...
)
}
\arguments{
\item{data}{A \code{PKNCAdata} object or a data.frame of intervals.}

\item{param}{The name of one secondary NCA parameter (see
\code{\link[=pknca_parameter_table]{pknca_parameter_table()}} for which parameters are secondary).}

\item{reference}{A data.frame describing the reference interval:  its
columns are \code{start}, \code{end}, and/or group columns of the intervals, every
column must match (and) for at least one of its rows (or).  A named list is
accepted and coerced.  When no interval matches, one is created and the
creation is reported with a \code{pknca_message_secondary_created_interval}
message.}

\item{target_groups}{A data.frame of group values restricting the parameter
request to matching intervals, with the same matching rules as \code{reference}.
\code{NULL} (the default) requests it on every interval that is not a reference
interval.}

\item{ref_id}{The \code{interval_id} to give the reference interval.  The default
of \code{NULL} keeps an identifier the reference rows already have and
otherwise generates one matching the class of the \code{interval_id} column.}

\item{...}{Ignored.}
}
\value{
The input with the parameter requested and the linkage columns set,
after \code{\link[=check.interval.specification]{check.interval.specification()}}.
}
\description{
A secondary parameter needs a result from a second profile:  renal clearance
divides an amount excreted in urine by a plasma AUC, an accumulation ratio
compares one dosing interval with another, and a metabolite ratio compares
two analytes.  This adds the request and the linkage columns
(\code{interval_id} on the reference interval and \verb{<param>_ref} on the intervals
calculating the parameter) that \code{\link[=pk.nca]{pk.nca()}} reads.
}
\details{
The reference interval gains whatever the linked calculation reads
from it (the plasma AUC for a renal clearance, for example) without being
announced, as calculating any dependency is.

An interval that already names a different reference for \code{param} is left
alone with a \code{pknca_warning_secondary_ref_exists} warning, so that the
helper never silently re-points an analysis.

When the parameter pairs an interval collection with spot-sample
references (renal clearance:  an amount excreted against a plasma AUC)
and the reference interval is created by the \code{PKNCAdata} method, the
created interval spans the collections whole:  a collection that begins
inside the interval contributes its full amount (see \code{\link[=pk.nca]{pk.nca()}}), so a
collection running past the interval's \code{end} extends the created
reference's \code{end} to \code{time + duration} of the latest collection.  An
explicit \code{end} in \code{reference} overrides the extension, and the data.frame
method (which has no concentration data) copies the test interval's times
unchanged.
}
\examples{
intervals <-
  data.frame(
    PCSPEC = c("plasma", "urine"),
    start = 0, end = 24,
    auclast = c(TRUE, FALSE),
    ae = c(FALSE, TRUE)
  )
interval_add_secondary(
  intervals,
  param = "clr.last",
  reference = data.frame(PCSPEC = "plasma")
)
}
\seealso{
\code{\link[=interval_add_param]{interval_add_param()}}, \code{\link[=pknca_ref]{pknca_ref()}}, \code{\link[=pk.nca]{pk.nca()}}

Other Interval specifications:
\code{\link[=add.interval.col]{add.interval.col()}},
\code{\link[=check.interval.specification]{check.interval.specification()}},
\code{\link[=choose.auc.intervals]{choose.auc.intervals()}},
\code{\link[=get.interval.cols]{get.interval.cols()}},
\code{\link[=get.parameter.deps]{get.parameter.deps()}},
\code{\link[=interval_add_impute]{interval_add_impute()}},
\code{\link[=interval_add_param]{interval_add_param()}},
\code{\link[=pknca_check_parameter_classification]{pknca_check_parameter_classification()}},
\code{\link[=pknca_concepts]{pknca_concepts()}},
\code{\link[=pknca_interval_table]{pknca_interval_table()}},
\code{\link[=pknca_parameter_table]{pknca_parameter_table()}},
\code{\link[=pknca_presets]{pknca_presets()}},
\code{\link[=pknca_ref]{pknca_ref()}}
}
\concept{Interval specifications}
