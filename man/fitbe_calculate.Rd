% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bioequivalence.R
\name{fitbe_calculate}
\alias{fitbe_calculate}
\title{Calculate bioequivalence statistics in one step}
\usage{
fitbe_calculate(
  object,
  endpoints = c("cmax", "aucinf.obs", "aucinf.pred", "auclast"),
  reference_col,
  reference_value,
  fixed = NULL,
  random = NULL,
  alpha = 0.1
)
}
\arguments{
\item{object}{A \code{PKNCAresults} object (preferred) or a data.frame in the long
format produced by calling \code{as.data.frame()} on a \code{PKNCAresults} object (a
\code{PPTESTCD} column of parameter names and a \code{PPORRES} or \code{PPSTRES} column of
values, alongside the grouping columns).}

\item{endpoints}{Character vector of NCA parameters (matched against
\code{PPTESTCD}) to model.}

\item{reference_col}{The name of the column identifying the
formulation/treatment.}

\item{reference_value}{The value of \code{reference_col} that is the reference
formulation.}

\item{fixed}{Optional fixed-effects right-hand side as a single string (for
example \code{"sequence + period + treatment"}).  When \code{NULL}, it is derived from
the grouping variables of a \code{PKNCAresults} object.}

\item{random}{Optional random-effects specification as a single string (for
example \code{"(1|subject)"}).  When \code{NULL}, it is derived from the subject
column.}

\item{alpha}{The significance level passed to \code{\link[=fitbe_table]{fitbe_table()}}; the confidence
interval has confidence level \code{1 - alpha}.}
}
\value{
A list with elements \code{table} (the \code{\link[=fitbe_table]{fitbe_table()}} data.frame) and
\code{fit} (the \code{\link[=fitbe_models]{fitbe_models()}} object).
}
\description{
\code{fitbe_calculate()} is a convenience wrapper that fits the bioequivalence
models with \code{\link[=fitbe_models]{fitbe_models()}} and summarizes them with \code{\link[=fitbe_table]{fitbe_table()}}.
}
\examples{
\dontshow{if (requireNamespace("lme4", quietly = TRUE) && requireNamespace("lmerTest", quietly = TRUE) && requireNamespace("emmeans", quietly = TRUE)) withAutoprint(\{ # examplesIf}
# A small 2x2 crossover example in PKNCAresults long format
set.seed(1)
n <- 12
sequence <- rep(c("RT", "TR"), length.out = n)
subj_effect <- stats::rnorm(n, sd = 0.3)
be_data <- do.call(rbind, lapply(seq_len(n), function(i) {
  forms <- if (sequence[i] == "RT") c("R", "T") else c("T", "R")
  mu <- log(100) + subj_effect[i] + ifelse(forms == "T", 0.05, 0)
  data.frame(
    subject = i, sequence = sequence[i], period = c(1, 2), form = forms,
    PPTESTCD = "auclast", PPORRES = exp(mu + stats::rnorm(2, sd = 0.1)),
    stringsAsFactors = FALSE
  )
}))
fitbe_calculate(
  be_data,
  endpoints = "auclast",
  reference_col = "form",
  reference_value = "R",
  fixed = "sequence + period + form",
  random = "(1|subject)"
)$table
\dontshow{\}) # examplesIf}
}
\seealso{
Other bioequivalence:
\code{\link{fitbe_models}()},
\code{\link{fitbe_table}()}
}
\concept{bioequivalence}
