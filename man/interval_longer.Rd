% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/intervals_support.R
\name{interval_longer}
\alias{interval_longer}
\alias{interval_wider}
\title{Convert intervals between the wide and long representations}
\usage{
interval_longer(intervals)

interval_wider(long, template)
}
\arguments{
\item{intervals}{A data.frame of intervals (the wide representation).}

\item{long}{The long representation, as returned by \code{interval_longer()}.}

\item{template}{The intervals data.frame the long form came from; used to
restore the column order and any parameter columns that are no longer
requested anywhere.}
}
\value{
\code{interval_longer()} gives one row per interval and requested
parameter with a \code{param} column naming the parameter.  \code{interval_wider()}
gives the wide representation back.
}
\description{
Convert intervals between the wide and long representations
}
\details{
Only parameters requested as \code{TRUE} become rows in the long form.
Round-tripping therefore normalizes \code{NA} to \code{FALSE}, which is required:
\code{NA} in a parameter column is rejected by
\code{\link[=check.interval.specification]{check.interval.specification()}} and stops \code{\link[=pk.nca]{pk.nca()}}.
}
\keyword{internal}
